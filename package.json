{
  "name": "coding-family-full-stack",
  "version": "1.0.0",
  "description": "",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "app:prod": "concurrently --kill-others \"yarn --cwd ./backend dev\" \"yarn --cwd frontend dev\"",
    "app:dev": "concurrently --kill-others \"yarn --cwd ./backend dev\" \"yarn --cwd frontend dev\"",
    "app:install": "yarn install && yarn --cwd ./backend install && yarn --cwd ./frontend install"
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
